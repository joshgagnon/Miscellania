#include <stdlib.h>
#include <stdio.h>
/*
struct point_t{
  double x;
  double y;
} point;

typedef point_t * point;

struct vector_t{
  double x;
  double y;
} vect;

typedef vector_t * vector;
*/

struct ray_t{
  double x,y;
  double dx,dy;
} ray;

int main(void){
  ray.x=0; ray.y=10.1;
  ray.dx=1.4; ray.dy=-9.6-10.1;
  double yori = 
  double y = (yori + ray.dx*(ray.dx-ray.x)); 
  //collide
  printf("%f\n", y);


  return 0;
}
